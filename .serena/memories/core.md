- Public API is re-exported from `astToolkit/__init__.py`; prefer importing from package root instead of private modules.
- Source map:
  - foundation tools: `_toolBe.py`, `_toolDOT.py`, `_toolGrab.py`, `_toolMake.py`
  - predicate/action layer: `_toolIfThis.py`, `_toolThen.py`
  - traversal layer: `_toolkitNodeVisitor.py` (`NodeTourist`, `NodeChanger`)
  - higher-level helpers: `_namespaceUncertainty.py`, `containers.py`, `transformationTools.py`
  - type/SSOT layer: `theTypes.py`, `_theSSOT.py`
- Tests live in `tests/`; deterministic fixtures/samples live in `tests/dataSamples/`.
- `notesClassFind/` and `Z0Z_notes/` are development/research notes, not core runtime API.
- Sibling workspace repo `c:\apps\astToolFactory` manufactures/supports toolkit patterns and depends on `astToolkit`; consult it when changes touch generated patterns, type-manufacturing, or factory-style AST helpers.
- Read `mem:conventions` for authoring rules, `mem:tech_stack` for tooling/config, `mem:suggested_commands` for common commands, and `mem:task_completion` for done criteria.