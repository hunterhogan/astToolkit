- Language/runtime: Python `>=3.13`.
- Packaging/build: `setuptools` via `pyproject.toml`; typed package ships `py.typed`.
- Runtime dependency: `hunterMakesPy>=0.4.3`.
- Test stack: `pytest`, `pytest-cov`, `pytest-xdist`.
- Static analysis:
  - `pyrightconfig.json` uses `typeCheckingMode = strict`
  - `ty.toml` is present with minimal config
  - `ruff.toml` line length `140`, single quotes, tabs, NumPy docstyle, many rules intentionally ignored
- Editor baseline from `.editorconfig`:
  - Python/default files: UTF-8, LF, tabs, tab width 4, final newline, trim trailing whitespace, max line length 140
  - `*.md`, `*.toml`, `*.yml`: UTF-8, LF, 2 spaces, final newline, trim trailing whitespace, max line length 102
- OS context for current dev environment: Windows.